
var binary=[0,1,0,0,1,1,0,0];
var binaryShift=[];


function encrypt(input, key){

    if( binary[binary.length] == 0){

        binary[binary.length] == 1;

    }else if( binary[binary.length] == 1){

        if( binary[binary.length-1] == 0){

            binary[binary.length-1] == 1;

            binary[binary.length] == 0;

        }else if( binary[binary.length] == )

    }

    return output

}

for(var i=0;i<binary.length;i++){
    binaryShift.push(encrypt(binary[i],1));
}